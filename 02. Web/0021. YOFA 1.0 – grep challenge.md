**Задание:**

Первая попытка запуска задачи. В приложенных исходниках есть лишние данные, с помощью которых решение очень простое.  
Задача перезапущена в v2, где исправлены исходники и поднят новый инстанс.

Адрес: http://109.172.39.144:3000

---
**Решение:**

Незапланированное решение №1 – в исходниках проекта в файле `init-db.js` оставлены креды от админа:

```js
const adminPassword = "S}>S3z'VH+r8u:}LFygl5GYe";
```

Незапланированное решение №2 – в исходниках проекта в файле `report.js` был оставлен ключ для подписи jwt-токена:

```js
const JWT_SECRET = process.env.JWT_SECRET || "supersecret";
```

Для получения флага регистрируем пользователя → получаем токен с ролью `user` → в jwt.io меняем роль на `admin` и подписываем токен → идём на ручку `/admin/flag` с новым токеном:

![Изображение](https://raw.githubusercontent.com/rst-temp/bug-makers/refs/heads/main/assets/Screenshot_9.png)